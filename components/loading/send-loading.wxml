<!--components/loading/send-loading.wxml-->
<view style='{{containerStyle}}' class='.loader .line-spin-fade-loader'>
  <block wx:for="{{itemStyleList}}" wx:for-item="itemStyle">
    <view style='{{itemStyle}}'></view>
  </block>
</view>